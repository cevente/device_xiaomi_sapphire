<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2023 The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
     Updated with proper AOD brightness mapping for 1800 nits display
-->

<resources xmlns:xliff="urn:oasis:names:tc:xliff:document:1.2">

    <!-- ==================== STRING ARRAYS ==================== -->

    <!-- Tiles to auto add to Quick Settings upon first change of a given secure setting -->
    <string-array name="config_quickSettingsAutoAdd" translatable="false">
        <item>accessibility_display_inversion_enabled:inversion</item>
        <item>wind_down_first_time_setup:custom(com.google.android.apps.wellbeing/.screen.ui.GrayscaleTileService)</item>
        <item>focus_mode_first_time_setup:custom(com.google.android.apps.wellbeing/.focusmode.quicksettings.FocusModeTileService)</item>
    </string-array>

    <!-- ==================== INTEGER ARRAYS ==================== -->

    <!-- Doze: Table that translates sensor values from the doze_brightness_sensor_type sensor
               to brightness values (mapped to 4095-scale backlight)
               -1 means keeping the current brightness -->
    <integer-array name="config_doze_brightness_sensor_to_brightness">
        <item>-1</item>    <!-- Sensor value 0: No change -->
        <item>-1</item>    <!-- Sensor value 1: No change -->
        <item>-1</item>    <!-- Sensor value 2: No change -->
        <item>1</item>     <!-- Sensor value 3: Minimum (1 nit) -->
        <item>200</item>   <!-- Sensor value 4: Low (approx 50 nits) -->
        <item>400</item>   <!-- Sensor value 5: Medium (approx 100 nits) -->
        <item>800</item>   <!-- Sensor value 6: Bright (approx 250 nits) -->
        <item>1200</item>  <!-- Sensor value 7: Very bright (approx 500 nits) -->
        <item>2048</item>  <!-- Sensor value 8: HBM (approx 800 nits) -->
        <item>3000</item>  <!-- Sensor value 9: High HBM (approx 1300 nits) -->
        <item>4095</item>  <!-- Sensor value 10: Peak (1800 nits) -->
    </integer-array>

    <!-- Doze: Table that translates sensor values to an opacity value for a black scrim
               overlayed in AOD. Valid range: 0 (transparent) to 255 (opaque).
               -1 means keeping the current opacity -->
    <integer-array name="config_doze_brightness_sensor_to_scrim_opacity">
        <item>-1</item>    <!-- Sensor 0: No change -->
        <item>255</item>   <!-- Sensor 1: Fully opaque (darkest) -->
        <item>200</item>   <!-- Sensor 2: Mostly opaque -->
        <item>150</item>   <!-- Sensor 3: Partially opaque -->
        <item>100</item>   <!-- Sensor 4: Semi-transparent -->
        <item>50</item>    <!-- Sensor 5: Mostly transparent -->
        <item>25</item>    <!-- Sensor 6: Very transparent -->
        <item>10</item>    <!-- Sensor 7: Barely visible -->
        <item>5</item>     <!-- Sensor 8: Almost transparent -->
        <item>2</item>     <!-- Sensor 9: Very slight tint -->
        <item>0</item>     <!-- Sensor 10: Fully transparent (brightest) -->
    </integer-array>

    <!-- ==================== BOOLS ==================== -->

    <!-- Doze: does this device support STATE_DOZE? -->
    <bool name="doze_display_state_supported">true</bool>

    <!-- Doze: Use brightness sensor for AOD brightness control -->
    <bool name="doze_use_brightness_sensor">true</bool>

    <!-- Doze: Enable dynamic AOD brightness -->
    <bool name="doze_dynamic_brightness_enabled">true</bool>

    <!-- ==================== DIMENS ==================== -->

    <dimen name="large_dialog_width">348dp</dimen>
          
    <!-- Pixel pitch (presumably in microns or similar units) -->
    <item type="dimen" name="pixel_pitch">64.41</item>

    <!-- ==================== INTEGERS ==================== -->

    <!-- The CPU temperature divider -->
    <integer name="config_cpuTempDivider" translatable="false">1000</integer>

    <!-- If not zero, limits the internal screen recorder's framerate to the set value -->
    <integer name="config_screenRecorderMaxFramerate">60</integer>

    <!-- ==================== STRINGS ==================== -->

    <!-- Type of a sensor that provides a low-power estimate of the desired display
         brightness, suitable to listen to while the device is asleep (e.g. during
         always-on display) -->
    <string name="doze_brightness_sensor_type" translatable="false">xiaomi.sensor.aod</string>

    <!-- ==================== ADDITIONAL AOD CONFIG ==================== -->

    <!-- Default AOD brightness level (mapped to 4095-scale) -->
    <integer name="config_doze_default_brightness">200</integer>

    <!-- Minimum AOD brightness -->
    <integer name="config_doze_minimum_brightness">1</integer>

    <!-- Maximum AOD brightness (capped to prevent battery drain) -->
    <integer name="config_doze_maximum_brightness">800</integer>

</resources>
